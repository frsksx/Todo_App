<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <FontFamily x:Key="AppFontFamily">Segoe UI Variable Text, Segoe UI</FontFamily>

    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F7F5F0"/>
    <SolidColorBrush x:Key="AppSurfaceBrush" Color="#FFFEFB"/>
    <SolidColorBrush x:Key="AppSurfaceMutedBrush" Color="#F0EEE8"/>
    <SolidColorBrush x:Key="AppSurfaceRaisedBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="AppBorderBrush" Color="#DFDAD0"/>
    <SolidColorBrush x:Key="AppBorderSoftBrush" Color="#ECE7DD"/>
    <SolidColorBrush x:Key="AppTextBrush" Color="#242B2E"/>
    <SolidColorBrush x:Key="AppTextMutedBrush" Color="#6C7378"/>
    <SolidColorBrush x:Key="AppTextSubtleBrush" Color="#92989C"/>
    <SolidColorBrush x:Key="AppAccentBrush" Color="#2F7D8C"/>
    <SolidColorBrush x:Key="AppAccentStrongBrush" Color="#1F6674"/>
    <SolidColorBrush x:Key="AppAccentSoftBrush" Color="#DDF2F2"/>
    <SolidColorBrush x:Key="AppAccentHoverBrush" Color="#EAF7F6"/>
    <SolidColorBrush x:Key="AppSelectedBrush" Color="#D8F0EF"/>
    <SolidColorBrush x:Key="AppHoverBrush" Color="#F0F8F7"/>
    <SolidColorBrush x:Key="AppWarningBrush" Color="#B96026"/>
    <SolidColorBrush x:Key="AppDangerBrush" Color="#C84A3D"/>
    <SolidColorBrush x:Key="AppDoneBrush" Color="#4D9B64"/>
    <SolidColorBrush x:Key="AppDisabledBrush" Color="#B7B9B7"/>

    <CornerRadius x:Key="AppCornerSmall">6</CornerRadius>
    <CornerRadius x:Key="AppCorner">9</CornerRadius>
    <CornerRadius x:Key="AppCornerLarge">14</CornerRadius>

    <Thickness x:Key="AppControlPadding">10,5</Thickness>
    <Thickness x:Key="AppTightControlPadding">8,3</Thickness>

    <Style TargetType="{x:Type Window}">
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="Background" Value="{StaticResource AppBackgroundBrush}"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <Style x:Key="AppLabelTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource AppTextMutedBrush}"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <Style x:Key="AppHintTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource AppTextSubtleBrush}"/>
    </Style>

    <Style x:Key="AppCardStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderSoftBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource AppCornerLarge}"/>
        <Setter Property="Padding" Value="14"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource AppControlPadding}"/>
        <Setter Property="MinHeight" Value="26"/>
        <Setter Property="Cursor" Value="Arrow"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource AppCorner}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource AppAccentHoverBrush}"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AppAccentSoftBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource AppSelectedBrush}"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AppAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource AppAccentBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource AppCorner}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource AppAccentStrongBrush}"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AppAccentStrongBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#174B55"/>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="#174B55"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="QuietButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderSoftBrush}"/>
        <Setter Property="Padding" Value="{StaticResource AppTightControlPadding}"/>
    </Style>

    <Style x:Key="TextInputStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="SelectionBrush" Value="{StaticResource AppAccentSoftBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource AppCorner}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                      VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AppAccentBrush}"/>
                            <Setter TargetName="Chrome" Property="Background" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextInputStyle}"/>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Foreground" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="MinHeight" Value="26"/>
    </Style>

    <Style TargetType="{x:Type DatePicker}">
        <Setter Property="Foreground" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{StaticResource AppTextMutedBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{StaticResource AppSurfaceRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
    </Style>

    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="Background" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppTextBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>
</ResourceDictionary>
